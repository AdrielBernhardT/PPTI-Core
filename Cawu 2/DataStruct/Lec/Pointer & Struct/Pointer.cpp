#include <stdio.h>

int main(){
    /*
    int i[10] = {21, 299, 129, 231, 42, 942, 831, 912, 141};
    int* point;
    point = &i[3];
    printf("%d", *point);

    int age = 20;
    int* pAge;
    pAge = &age;
   
    int newage = 50;
    int* newpAge;
    newpAge = pAge;
    newpAge = &newage;
    printf("pAge = %d -> address = %p\n", *pAge, pAge);
    printf("age address = %p\n", &age);
    puts("");
    printf("newpAge = %d -> address = %p\n", *newpAge, newpAge);
    printf("newAge address = %p\n", &newage);

    int i = 1;
    int *p, *q;
    p = &i;
    q = p;
    *q = 5;
    printf("%x\n", p); // Hexa
    printf("%p\n", p); // Address
    printf("%d\n", *p); // Value dari Address tersebut

    */
    return 0;
}
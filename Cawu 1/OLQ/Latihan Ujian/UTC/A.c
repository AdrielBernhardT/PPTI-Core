#include <stdio.h>

int main(){
    int x, y,  command;
    char gerak[1005];
    scanf("%d %d", &x, &y);
    scanf("%d", &command);
    scanf("%s", gerak);
    for (int i = 1; i <= command; i++)
    {
        
    }
    

    return 0;
}